---
import integrants from "@/data/integrants";
import { Image } from "astro:assets";
---

<section class="w-full py-12 md:py-24 lg:py-16 bg-muted">
  <div class="container mx-auto px-4 md:px-6">
    <div
      class="flex flex-col items-center justify-center space-y-4 text-center"
    >
      <div class="space-y-2">
        <div class="inline-block rounded-lg bg-muted px-3 py-1 text-sm">
          Nuestro Equipo
        </div>
        <h2 class="text-3xl font-bold tracking-tighter sm:text-5xl">
          Conoce a Nuestros Integrantes
        </h2>
        <p
          class="max-w-[900px] mx-auto text-muted-foreground md:text-xl/relaxed lg:text-base/relaxed xl:text-xl/relaxed text-center"
        >
          Nuestro equipo de expertos en tecnología, ciencia de datos y gestión
          de riesgos trabaja incansablemente para desarrollar soluciones que
          ayuden a nuestros clientes a proteger sus operaciones y a sus
          empleados ante los desastres naturales.
        </p>
        <div class="px-4 md:px-6 py-12 grid grid-cols-2 md:grid-cols-4 gap-6">
          {
            integrants.map((integrant) => (
              <article class="flex flex-col items-center justify-center space-y-2">
                <Image
                  src={integrant.avatar}
                  alt={integrant.name}
                  class:list={[
                    "rounded-full size-28 object-cover",
                    integrant.className,
                  ]}
                />
                <div class="text-center">
                  <p class="font-medium text-base">{integrant.name}</p>
                </div>
              </article>
            ))
          }
        </div>
      </div>
    </div>
  </div>
</section>
